# このリポジトリを編集するAI・開発者へ

サイトはTypeSafe AI / Jevの非公式で独立した日本語学習教材です。公式な監修・公認・提携を示唆しないでください。ユーザーは正確さと日本語の分かりやすさを重視します。

## 検証と出典

仕様を変更する前に公式ドキュメントを再確認し、該当記事の出典を更新してください。モデルID・料金・制限・SDK・日本語精度を記憶で断定しません。確認日は実際に確認した日です。独自の入力例、期待値、仮のしきい値、推測と実測を区別します。

実API通信、日本語精度、SDKの実行、GitHub Pagesの公開は初版作成時点で未検証です。後から実行した場合も、成功した範囲だけVALIDATION.mdに記録してください。

## 安全な設計

配信するコード・HTML・JSON・ログにAPIキーを入れません。公開サイトは推論を行わず、API通信をしない構成を維持します。APIの実行が必要なら別のローカル／サーバー実習として設計します。実行権限と送信データと費用を確認してください。顧客データや業務秘密は教材に使いません。

`npm run example:mock`は固定の人工データです。モデルの精度や性能を証明する値ではありません。Noulに存在しないconfidenceを付けたり、Choice / Scoreのconfidenceを推定して実測値のように扱ったりしないでください。

## 編集とコマンド

本文はcontent/*.md、メタデータはcontent/manifest.json、出典はcontent/sources.jsonです。distは直接修正しません。npm testとnpm run buildを実行してください。依存なしのNode.js 22以上で動きます。

Pythonのtest/browser-check.pyは任意の追加テストです。playwrightとChromiumがある環境で単体プレビューを検証します。npm testやGitHub Actionsの標準テストには追加依存を要求しません。

学習順は「まず試す→概念→実装→応用→品質」を守ります。専門用語は初出で説明し、日本語の文を省略しすぎないでください。各章の目的・例・注意点・確認問題を大切にします。公式文の大量翻訳転載に置き換えないでください。

## 公開

初版作成環境からのリポジトリ作成・push・Pages公開はしていません。権限を確認し、明示的な公開操作が成功した後だけ実際のURLを公開済みとして伝えます。既存リポジトリを上書き・削除したり、force pushしたりしません。
